# Traffic police overspeeding check

speed = float(input("Enter vehicle speed (km/h): "))

if speed > 60:
    print("Overspeeding! You have to pay a fine.")
else:
    print("You are within the speed limit.")
